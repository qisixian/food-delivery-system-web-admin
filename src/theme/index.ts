import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
    palette: {
        background: {
            default: "#f3f4f7",
            paper: "#ffffff",
        },
        primary: {
            main: "#ffc100",
        },
        secondary: {
            main: "#1976d2",
        }
    },
});